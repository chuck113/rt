package com.rt.indexing



object HierarchicalIndexerObj{

  def main(args:Array[String]):Unit={
    val hierarchicalIndexer:HierarchicalIndexer = new HierarchicalIndexer()
     val toIndex = List("Public_Enemy")
    //println(split(List(1, 2, 3, 4, 5, 6, 7, 8, 9),2));
    //val artistNode: ArtistNode = indexer.makeArtistNode(OhhlaConfig.rawTargetLocation + "/" + "Nas")
    //artistNode.children.foreach(album => println(album.children))
    //val artistNodes:Map[String, ArtistNode] = hierarchicalIndexer.makeArtistsHierarchy(toIndex)
    //hierarchicalIndexer.serializeHierarcyInBatches(artistNodes, 3)
    null
  }
}